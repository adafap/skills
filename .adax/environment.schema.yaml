{
  "schemaVersion": 1,
  "environments": {
    "development": { "valueSource": ".env.local" },
    "preview": { "valueSource": "GitHub Environment Preview" },
    "production": { "valueSource": "GitHub Environment Production" }
  },
  "variables": [],
  "isolation": { "requiredVariables": [] }
}

